import {
  Wrench,
  Image,
  Scissors,
  Wand2,
  Sparkles,
  FileText,
  Type,
  Code2,
  Crop,
  Palette,
  Eraser,
  QrCode,
  Link as LinkIcon,
  Calculator,
  FileImage,
  FileArchive,
  Ruler,
  Droplet,
  Bot,
  Repeat,
  Scaling,
  Braces,
  KeyRound,
  Binary,
  Hash,
  ScanText,
  FileJson,
  Table,
  FileCode2,
  FileCode,
  Database,
  Terminal,
  Fingerprint,
  LockKeyhole,
  Lock,
  ShieldCheck,
  GitBranch,
  Dices,
  Clock,
  CalendarClock,
  Network,
  Contrast,
  Regex,
  GitCompare,
  CaseSensitive,
  Quote,
  WrapText,
  type LucideProps,
} from "lucide-react";

// Curated set so we don't bundle every lucide icon. Add more as tools need them;
// the admin "icon" field accepts any of these names (case-sensitive).
const ICONS: Record<string, React.ComponentType<LucideProps>> = {
  Wrench,
  Image,
  Scissors,
  Wand2,
  Sparkles,
  FileText,
  Type,
  Code2,
  Crop,
  Palette,
  Eraser,
  QrCode,
  Link: LinkIcon,
  Calculator,
  FileImage,
  FileArchive,
  Ruler,
  Droplet,
  Bot,
  Repeat,
  Scaling,
  Braces,
  KeyRound,
  Binary,
  Hash,
  ScanText,
  FileJson,
  Table,
  FileCode2,
  FileCode,
  Database,
  Terminal,
  Fingerprint,
  LockKeyhole,
  Lock,
  ShieldCheck,
  GitBranch,
  Dices,
  Clock,
  CalendarClock,
  Network,
  Contrast,
  Regex,
  GitCompare,
  CaseSensitive,
  Quote,
  WrapText,
};

export function ToolIcon({
  name,
  ...props
}: { name?: string | null } & Omit<LucideProps, "name">) {
  const Icon = (name && ICONS[name]) || Wrench;
  return <Icon {...props} />;
}
